builds:
  - id: service
    dir: . # default is .
    main: ./cmd/server/
    # env:
    #   - GOPRIVATE=git.internal.example.com,source.developers.google.com
    # flags:
    #   - -tags
    #   - netgo
    # ldflags:
    #   - -s -w
    #   - -extldflags "-static"
    #   - -X main.version={{.Env.VERSION}}
defaultPlatforms:
  - all
  # - linux/arm64
  # - linux/amd64
